# Express API (server/). Next.js uses Dockerfile.next at repo root (not `Dockerfile`) so Railway does not auto-pick the wrong image.
[build]
builder = "DOCKERFILE"
dockerfilePath = "server/Dockerfile"

[deploy]
healthcheckPath = "/api/health"
